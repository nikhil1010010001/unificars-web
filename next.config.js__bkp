/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
   apps: [
        {
            name: "inventory_test",
            script: "node_modules/next/dist/bin/next",
            args: "start -p 3000", //running on port 3000
            watch: false,
        },
    ],	
  images: {
    domains: ["crm.unificars.com", "unificars.com"],
  },
}

module.exports = nextConfig
